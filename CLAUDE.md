# ReShare

Android app that receives shared documents, converts them via Pandoc, and reshares to other targets.

## Build

```
./gradlew <task>
```

## Planning

Project planning lives in `.plan/` — see `.plan/WORKFLOW.md` for task execution steps.
Key files: `PLAN.md`, `BACKLOG.md`, `NOTES.md`, `specs/`, `prds/`, `reviews/`, `archive/`.
